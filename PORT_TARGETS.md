# Port targets — `age-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `age` that codex-rs uses, and therefore the minimum subset that `age-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-8] (rank `226`, tier `direct`, status `To Do`)

**Cargo package name**: `age`
**Rust module name (in `use` statements)**: `age`

## Summary

- **6** distinct symbols imported by codex-rs
- **6** total `use` statements
- **1** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `age::decrypt`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-secrets`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `secrets/src/local.rs:11` (`codex-secrets`) — `use age::decrypt`

### `age::encrypt`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-secrets`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `secrets/src/local.rs:13` (`codex-secrets`) — `use age::encrypt`

### `age::scrypt::Identity`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-secrets`
- Aliases used: `as ScryptIdentity`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `secrets/src/local.rs:14` (`codex-secrets`) — `use age::scrypt::Identity as ScryptIdentity`

### `age::scrypt::Recipient`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-secrets`
- Aliases used: `as ScryptRecipient`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `secrets/src/local.rs:15` (`codex-secrets`) — `use age::scrypt::Recipient as ScryptRecipient`

### `age::secrecy::ExposeSecret`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-secrets`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `secrets/src/local.rs:16` (`codex-secrets`) — `use age::secrecy::ExposeSecret`

### `age::secrecy::SecretString`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-secrets`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `secrets/src/local.rs:17` (`codex-secrets`) — `use age::secrecy::SecretString`
